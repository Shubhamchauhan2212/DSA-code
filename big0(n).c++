#include <bits\stdc++.h>
using namespace std;

int main()
{
    vector<int> arr = {10, 9, 5, 4, 8};
    int n = arr.size();
    // int sum = 0;
    for (int i = 0; i < n; i++)
    {
        cout << arr[i] << " ";
    }

    cout << endl;
    return 0;
}